### 安装依赖

```bash
# 安装依赖 (推荐使用 pnpm)
pnpm install

# 或使用 npm
npm install
```

### 开发环境

```bash
# 启动开发服务器
pnpm dev

# 服务器将在 http://localhost:3001 启动
```

### 构建部署

```bash

# 测试环境构建  
pnpm build:test

# 生产环境构建
pnpm build:pro

```