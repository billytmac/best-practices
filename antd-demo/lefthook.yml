# lefthook.yml
pre-commit:
  parallel: true # 平行執行所有 commands
  commands:
    # lint:
    #   glob: "*.{js,jsx,ts,tsx,mjs,cjs}"
    #   run: npx oxlint {staged_files} --fix && npx eslint {staged_files} --fix
    #   stage_fixed: true # 自動把修正後的檔案加回 staging
    # format:
    #   glob: "*.{ts,tsx,json,md}"
    #   run: npx prettier --write {staged_files}
    #   stage_fixed: true
    # typecheck:
    #   run: npx tsc --noEmit
commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit {1}
